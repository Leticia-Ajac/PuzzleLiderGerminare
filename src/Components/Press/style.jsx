import { Link } from "react-router-dom";
import styled from "styled-components";

export const Press = styled(Link)({
    textDecoration:'none',
    color:'#fff', 
    margin:'37px auto 0',
    fontSize:'24px',
})
